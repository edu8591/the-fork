import { RestaurantCreateForm } from "@/components";

export default function RestaurantCreatePage() {
  return (
    <main className="h-full flex items-center justify-center">
      <RestaurantCreateForm />
    </main>
  );
}
